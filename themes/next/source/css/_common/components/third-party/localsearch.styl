.local-search {
  position: relative;
  text-align: left;
  margin-bottom: 10px;
}


.site-search-div {
  /* display: relative; */
  /* align-items: center; 垂直居中对齐 */
  position: relative;
}

.local-search-overlay {
  position: fixed;
  top: 0;
  right: 0;
  bottom: 0;
  left: 0;
  opacity: 0;
  transition: opacity 0.6s;
  will-change: opacity;
  z-index: $zindex-6;
  background: #000;
  pointer-events: none; /* 让点击穿透 */
}

.local-search-overlay.show {
  opacity: 0.6;
  pointer-events: auto; /* 不让点击穿透 */
}

.search-icon {
  position: absolute;
  left: -20px;
  top: -1px;
  background: transparent;
  border: none;
  font-size: 18px;
  color: var(--link-color);
  pointer-events: none; /* 避免点击 */

  +mobile() {
    pointer-events: auto;
    transform: translateY(-50%);  /* 垂直居中 */
    left: 0px;  /* 控制按钮距离输入框左侧的距离 */
    top: 50%;    /* 垂直居中 */
    cursor: pointer;
    padding: 0;
    z-index: 1;
    /* search-icon要覆盖整个input框, 因为input框在手机上经常点不动, 所以search-icon的长度要 等于 input框的宽度+其左右padding(41px + 41px = 82px) 的 和 */
    /* width: calc(var(--local-search-width) + 82px); */
    height: 100%;
    /* 取消双击点击时的黑色高亮, 防止在触摸屏设备上双击时出现黑框。 */
    -webkit-tap-highlight-color: transparent;
  }
  +desktop-large() {
    top: 2px;
  }
  //@media (hover: hover) {
    &:hover {
      color: var(--link-hover-color);
    }
  //}
}

.search-icon i {
  position: absolute;
  top: 10px;

  +mobile() {
    left: 12px;
    top: 12px;
  }
}


input[id$=local-search-input] {
  padding: 0px 23px 1px 12px;
  border: none;
  text-indent: 14px;
  border-radius: 0;
  width: 98px;
  /* background: inherit;  */

  outline: none;
  box-shadow: none;
  /* 取消双击点击时的黑色高亮, 防止在触摸屏设备上双击时出现黑框。 */
  -webkit-tap-highlight-color: transparent;

  /* border-bottom: 1px solid $grey-dark; */
  /* z-index: 9999; */

  /*
  mobile() {
    @media (max-width: 767px) {
      {block}
    }
  }

  tablet() {
    @media (min-width: 768px) and (max-width: 991px) {
      {block}
    }
  }

  desktop() {
    @media (min-width: 992px) {
      {block}
    }
  }
  */

  @media (min-width: 768px) {
    /* transform: scaleX(0.8); */
    /* the-transition(); */
    background-image: -webkit-gradient(linear, left top, left bottom, from($grey-dark), to($grey-dark)), -webkit-gradient(linear, left top, left bottom, from(#d2d2d2), to(#d2d2d2));
    background-image: -webkit-linear-gradient($grey-dark, $grey-dark), -webkit-linear-gradient(#d2d2d2, #d2d2d2);
    background-image: linear-gradient($grey-dark, $grey-dark), linear-gradient(#d2d2d2, #d2d2d2);
    background-color: transparent;
    background-repeat: no-repeat;
    background-position: center bottom, center -webkit-calc(100% - 1px);
    background-position: center bottom, center calc(100% - 1px);
    background-size: 0 1px, 100% 1px;
    -webkit-transition: background 0.3s ease-out;
    transition: background 0.3s ease-out;
  }

  +mobile() {
    text-indent: 0px;
    -webkit-appearance: none;
    appearance: none;
    height: 43px;
    font-size: 12px;
    border-bottom: none;
    border-radius: 12px;
    background: var(--local-search-bg-color);
    /* box-shadow: inset 0.475em 0.375em 0.45em 0 #808080; */

    opacity: 0;
    /* padding: 0 0px 0 0px; */
    padding: 0 41px 0 41px;
    /* max-width: 0%; */
    /* max-width: 0px; */
    /* will-change: max-width, padding;  */
    will-change: opacity;  /* 提前优化动画 */
    /* width: 156px; */
    /* width: 100%; */
    width: calc(var(--local-search-width));
    /* transition: max-width 0.4s ease-out, padding 0.4s ease; 平滑过渡 */
    transition: opacity 0.4s ease-out; /* 平滑过渡 */
    overflow: visible;
  }

};

input[id$=local-search-input].expanded {
    /* width: 156px; */
    /* transform: scaleX(1); 展开至原始宽度 */
    /* padding: 0 41px 0 41px; */
    /* max-width: calc(var(--local-search-width)); */
    /* max-width: 156px; */
    opacity: 1;
}

/* input[id$=local-search-input]:hover { */
  /* @media (min-width: 768px) { */
    /* border-bottom: 1px solid rgb(15, 15, 15); */
    /* width: 100px; */
    /* transform: scaleX(1); */
    /* background-size: 100% 1px, 100% 1px; */
  /* } */
/* } */

/* input[id$=local-search-input]:focus { */
  /* @media (min-width: 768px) { */
    /* border-bottom: 1px solid rgb(15, 15, 15); */
    /* width: 100px; */
    /* transform: scaleX(1); */
    /* background-size: 100% 1px, 100% 1px; */
  /* } */
/* } */



.local-search-input-cls {
  width: 200px;
  margin: 10px 0;
  padding: 8px 12px;
  border-radius: 4px;
  border: 1px solid #CCC;
  color: #666;
  font-size: 14px;
}

.site-search-form {
  input {
    &::-ms-clear {
      display: none;
    }
  }
}

#local-search-close {
  content: 'x';
  position: absolute;
  right: -11px;
  top: 6px;
  opacity: 0;
  background: #888;
  color: #fff;
  border-radius: 100%;
  line-height: 19px;
  text-align: center;
  font-size: 16px;
  font-family: consolas;
  border-radius: 15px;
  /* border: 1px solid #CCC; */
  /* display: block; */
  width: 20px;
  height: 20px;
  cursor: pointer;
  font-style: normal;
  font-weight: 900;
  transform: rotateZ(0);
  pointer-events: none; /* 避免点击 */
  will-change: transform, opacity;
  transition: transform 0.5s ease-in-out, opacity 0.5s ease;
  /* display: none; */

  +mobile() {
    /* right: -75px; */
    right: calc(100% - var(--local-search-width) - 41px - 30px);  /* 这个41px是input框的左padding, 30px是close该有的offset */
    top: 11px;
    z-index: 2;
    transform: translateX(0);
  }

  &:hover {
    background: #fff;
    color: #888;
    /* border-color: #666; */
    transform: rotateZ(180deg);
    transition: all 0.3s;
  }
}

#local-search-close.show {
  opacity: 1; /* 透明度变为 1 */
  pointer-events: auto;  /* 默认行为，元素可以响应鼠标点击、悬停等事件。 */
}

.local-search-result-cls {
  position: fixed;
  z-index: $zindex-7;
  width: 400px;
  top: 90px;
  right: 0;

  /* -webkit-transform: translateY(-100%);
  transform: translateY(-100%);
  -webkit-transition: -webkit-transform 0.8s ease-in-out;
  transition: transform 0.8s ease-in-out; */

  +mobile() {
    top: 100px
    padding: 0
    left: 3%
    margin: 0
    width: 94%
    /* height: 100% */
    border-radius: 0
  }

  .local-search-loading {
    color: #888;
    /* line-height: 44px; */
    text-align: left;
    display: block;
    font-size: 13px;
    font-weight: normal;
    margin: 28px;
  }

  .local-search-empty {
    color: #888;
    line-height: 44px;
    text-align: center;
    display: block;
    font-size: 50px;
    font-weight: 900;
    margin: 58px;
  }


  ul {
    width: 450px;
    height: 600px;
    max-height: 650px;
    min-height: 0;


    overflow-y: auto;
    /* 当滚动到这个div的底部之后页面也不能滚动 */
    /* overscroll-behavior: contain; */
    border-radius: 15px;
    border: 0px solid #CCC;
    padding: 50px;
    background: var(--local-search-bg-color);
    /* box-shadow: 0.625em 0.625em 0.75em 0 #ccc; */
    /* box-shadow: 2.475em 2.375em 9.75em 0 #555;; */
    box-shadow: 3px 4px 16px rgba(0, 0, 0, 0.65);

    +mobile() {
      max-height: none;
      left: 0;
      margin: 0;
      width: initial;
      height: 60vh;
      /* box-shadow: 2.475em 2.375em 2.75em 0 #555;; */
    }
    +tablet() {
      width: 400px;
    }
    li {
      text-align: left;
      //border-bottom: 1px solid #CCC;
      padding-bottom: 20px;
      margin-bottom: 20px;
      line-height: 30px;
      font-weight: normal;

      &:last-child {
        //border-bottom: none;
        margin-bottom: 80px;
      }
      a {
        margin-top: 20px;
        font-size: 16px;
        &:focus {
          border-bottom-color: transparent; /* 当一个<a>被tab选中希望他的border-bottom变透明 */
        }
      }
      p {
        margin-top: 10px;
        font-size: 14px;
        max-height: 124px;
        overflow: hidden;
      }
      em.search-keyword {
        color: #e58c7c;
      }
    }
  }
}


.local-search-result-cls.active {
  -webkit-transform: translateY(0);
  transform: translateY(0);
}


.local-search-result-cls ul:before {
  content: "";
  position: absolute;
  border: 30px solid var(--local-search-bg-color);
  border-top: 0 solid transparent !important;
  border-right: 30px solid transparent !important;
  border-left: 30px solid transparent !important;
  right: 90px;
  top: -11px;


  +mobile() {
    border-right: 8px solid transparent !important;
    /* border-left: 13px solid transparent !important; */
    right: initial;
    top: -30px;
  }
}

.local-search-result-cls .search-keyword {
  /* border-bottom: 1px dashed rgb(219, 127, 13); */
  font-weight: bold;
  color: var(--local-search-key-word-color);
  /* color: var(--selection-color); */
  /* background-color: var(--selection-bg); */
}

.local-search-plugin {
  .local-search-input-cls {
    opacity: 0.6;
    width: 160px;
    transition: all 0.3s;
    &:hover {
      opacity: 1;
      width: 200px;
      transition: all 0.3s;
    }
  }
  .icon {
    position: relative;
    left: -30px;
    color: #999;
    cursor: pointer;
  }
}



/* loading css1 : Dot Css Loader*/
.loader:before,
.loader:after,
.loader {
  border-radius: 50%;
  width: 1em;
  height: 1em;
  -webkit-animation-fill-mode: both;
  animation-fill-mode: both;
  -webkit-animation: load7 1.8s infinite ease-in-out;
  animation: load7 1.8s infinite ease-in-out;
}
.loader {
  top: -27px;
  margin: -1em auto;
  /* font-size: 10px; */
  position: relative;
  text-indent: -9999em;
  -webkit-animation-delay: -0.16s;
  animation-delay: -0.16s;
}
.loader:before {
  left: -3.5em;
  -webkit-animation-delay: -0.32s;
  animation-delay: -0.32s;
}
.loader:after {
  left: 3.5em;
}
.loader:before,
.loader:after {
  content: '';
  position: absolute;
  top: 0;
}
@-webkit-keyframes load7 {
  0%,
  80%,
  100% {
    box-shadow: 0 2.5em 0 -1.3em #888888;
  }
  40% {
    box-shadow: 0 2.5em 0 0 #888;
  }
}
@keyframes load7 {
  0%,
  80%,
  100% {
    box-shadow: 0 2.5em 0 -1.3em #888888;
  }
  40% {
    box-shadow: 0 2.5em 0 0 #888;
  }
}


/* loading css2 : Pure Css Loader - Square */
/* @-webkit-keyframes enter {
  0% {
    opacity: 0;
    top: -10px;
  }
  5% {
    opacity: 1;
    top: 0px;
  }
  50.9% {
    opacity: 1;
    top: 0px;
  }
  55.9% {
    opacity: 0;
    top: 10px;
  }
}
@keyframes enter {
  0% {
    opacity: 0;
    top: -10px;
  }
  5% {
    opacity: 1;
    top: 0px;
  }
  50.9% {
    opacity: 1;
    top: 0px;
  }
  55.9% {
    opacity: 0;
    top: 10px;
  }
}
@-moz-keyframes enter {
  0% {
    opacity: 0;
    top: -10px;
  }
  5% {
    opacity: 1;
    top: 0px;
  }
  50.9% {
    opacity: 1;
    top: 0px;
  }
  55.9% {
    opacity: 0;
    top: 10px;
  }
}

* {
  margin: 0;
}

.loader {
  position: absolute;
  left: 50%;
  top: 90px;
  margin-left: -27.5px;
  margin-top: -27.5px;
}

.square {
  background: white;
  width: 10px;
  height: 10px;
  float: left;
  top: -10px;
  margin-right: 5px;
  margin-top: 5px;
  position: relative;
  opacity: 0;
  -webkit-animation: enter 6s infinite;
  animation: enter 6s infinite;
}

.enter {
  top: 0px;
  opacity: 1;
}

.square:nth-child(1) {
  -webkit-animation-delay: 1.8s;
  -moz-animation-delay: 1.8s;
  animation-delay: 1.8s;
}

.square:nth-child(2) {
  -webkit-animation-delay: 2.1s;
  -moz-animation-delay: 2.1s;
  animation-delay: 2.1s;
}

.square:nth-child(3) {
  -webkit-animation-delay: 2.4s;
  -moz-animation-delay: 2.4s;
  animation-delay: 2.4s;
  background: #1fbeca;
}

.square:nth-child(4) {
  -webkit-animation-delay: 0.9s;
  -moz-animation-delay: 0.9s;
  animation-delay: 0.9s;
}

.square:nth-child(5) {
  -webkit-animation-delay: 1.2s;
  -moz-animation-delay: 1.2s;
  animation-delay: 1.2s;
}

.square:nth-child(6) {
  -webkit-animation-delay: 1.5s;
  -moz-animation-delay: 1.5s;
  animation-delay: 1.5s;
}

.square:nth-child(8) {
  -webkit-animation-delay: 0.3s;
  -moz-animation-delay: 0.3s;
  animation-delay: 0.3s;
}

.square:nth-child(9) {
  -webkit-animation-delay: 0.6s;
  -moz-animation-delay: 0.6s;
  animation-delay: 0.6s;
}

.clear {
  clear: both;
}

.last {
  margin-right: 0;
} */



/* loading css3 : progress bar */
/****START PROGRESS****/
.progress-bar {
  top: 10px;
  margin: -1em auto;
  position: relative;
  height: 10px;
  width: 140px;
  border: solid 1px;
  color: #999;
}

.progress-bar-charge {
  height: 10px;
  background-color: #999;
  animation: charge 8s infinite;
  -webkit-animation: charge 8s infinite;
}

@-webkit-keyframes charge {
  0% {width : 0px;}
  100% {width : 101%;}
}

@keyframes charge {
  0% {width : 0px;}
  100% {width : 101%;}
}
/****FIN PROGRESS****/
